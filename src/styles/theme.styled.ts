import { color } from './color.styled';

export const theme = {
  colors: {
    default: color.gray[400],
    inverse: color.white,
    primary: color.green[400],
    secondary: color.red[400],
  },
};
